<%- include("template/headers",{tittle:'CRUD image'}) %>
<%- include("template/navbar") %>

<div class="container my-auto p-2">
  <div class="row d-flex align-items-center">    
      <div id="carouselExample" class="carousel slide">
        <div class="carousel-inner">  
          <% images.forEach(function(image, index) { %>
            <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
              <img src="./uploads/img/<%= image %>" class="d-block"  alt="Imagen <%= index + 1 %>">
            </div>
          <% }); %>
         </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>    
  </div>
</div>
<button>recargar</button>
apartado a recargar:  <%- include("template/navbar") %>


<!-- <script src="./js/upload_crud.js"></script> -->
<%- include("template/footer") %>

